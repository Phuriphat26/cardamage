name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

# ส่วนนี้คือที่ที่ถูกต้องของเวอร์ชัน SDK ครับ
environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2 
  path_provider: ^2.1.3 
  permission_handler: ^11.3.1 # สำหรับจัดการสิทธิ์การเข้าถึงไฟล์

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/